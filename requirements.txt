# The God Factory Video Editor — Python Dependencies
# Install with: pip install -r requirements.txt

# GUI Framework (Qt for Python — official binding, May 2026 stable)
PySide6>=6.7.0

# Video processing (FFmpeg Python bindings)
ffmpeg-python>=0.2.0

# Automatic scene detection
scenedetect[opencv]>=0.6.4

# Image processing (thumbnails, frame handling)
Pillow>=10.1.0
opencv-python>=4.9.0.80
numpy>=1.24.3

# System monitoring (memory, disk usage)
psutil>=5.9.6
